# AdPilot Chrome extension

Placeholder — built in Phase 7 (`04_PHASES.md`). Plain HTML/JS popup (Manifest
V3): CSV upload control, phone number field, submit button. Calls the same
backend API as the web app — no live Google Ads scraping/OAuth (out of scope,
see `01_PROJECT_REQUIREMENTS.md` section 5).
